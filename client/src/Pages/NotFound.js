import React from 'react';
// pass props and set error on data.message.error if error server 
// or not found id page doesn t exist 

function NotFound(){
  return(
    <div>
      <p>Not found page</p>
    </div>
  )
} 
export default NotFound;